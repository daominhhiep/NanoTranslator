{
  "name": "nanotranslator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "zip": "npm run build && cd dist && zip -r ../nanotranslator-chrome-store.zip .",
    "test": "vitest run",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.305",
    "@types/dom-chromium-ai": "^0.0.10",
    "@types/node": "^25.6.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.3"
  }
}
